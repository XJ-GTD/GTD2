
export enum EventType {
  Agenda = "0",
  Task = "1",
  MiniTask = "2"
}

export enum SyncType {
  unsynch = "unsynch",
  synch = "synch"
}

export enum DelType {
  undel = "undel",
  del = "del"
}

export enum GsType {
  self = "0",
  him = "1",
  sys = "2",
  waitin = "3",
  waitdel = "4"
}

export enum ObType {
  event = "event",
  memo = "memo",
  calendar = "calendar"
}
