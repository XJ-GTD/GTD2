{
  "_id": ObjectID("5e05c0a012876b0007060956"),
  "name": "冥王星接口方向同步调用",
  "trigger": "mwxing_api_proxy",
  "parameters": [
    "method",
    "path",
    "params",
    "header",
    "querys",
    "body",
    "callback"
  ],
  "follows": [
    {
      "name": "本地调用服务接口",
      "trigger": "exc",
      "parameters": [
        "method;$.parent.parameters.method",
        "urlabs;$.parent.parameters.path",
        "header;$.parent.parameters.header",
        "params;$.parent.parameters.params",
        "querys;$.parent.parameters.querys",
        "callback;$.parent.parameters.callback",
        "data;$.parent.parameters.body"
      ]
    }
  ]
}
