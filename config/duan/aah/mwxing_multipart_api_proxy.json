{
  "_id": ObjectID("5e0866ec12876b000706095a"),
  "name": "冥王星文件存储反向同步调用",
  "trigger": "mwxing_multipart_api_proxy",
  "parameters": [
    "storeId",
    "method",
    "path",
    "body",
    "callback"
  ],
  "follows": [
    {
      "name": "本地调用服务接口",
      "trigger": "exc",
      "parameters": [
        "bridge;binary",
        "method;$.parent.parameters.method",
        "urlabs;$.parent.parameters.path",
        "header;$.parent.parameters.header",
        "callback;$.parent.parameters.callback",
        "data;$.parent.parameters.body"
      ]
    }
  ]
}
